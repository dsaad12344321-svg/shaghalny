type Props = {
  previous?: string;
  next?: string;
};

export default function NavigationButtons({
  previous,
  next,
}: Props) {
  return (
    <div>
      {/* Previous */}

      {/* Next */}
    </div>
  );
}