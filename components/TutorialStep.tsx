type Props = {
  image: string;
  text: string;
};

export default function TutorialStep({
  image,
  text,
}: Props) {
  return (
    <section>
      {/* Image */}

      {/* Text */}
    </section>
  );
}